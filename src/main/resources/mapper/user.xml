<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.jeju.user.model.mapper.UserMapper">

	<insert id="insertUser">
		INSERT INTO user (user_id, user_name, user_password, user_email)
		VALUES (#{userId}, #{userName}, #{userPassword}, #{userEmail})
	</insert>

	<select id="selectUserByUserId" resultType="com.ssafy.jeju.user.model.dto.User">
		SELECT * FROM user WHERE user_id = #{userId}
	</select>

	<select id="selectAllUsers" resultType="com.ssafy.jeju.user.model.dto.User">
		SELECT * FROM user
	</select>

	<update id="updateUser">
		UPDATE user
		SET user_id = #{userId}, user_name = #{userName}, user_password = #{userPassword}, user_email = #{userEmail}
		WHERE id = #{id}
	</update>

	<delete id="deleteUser">
		DELETE FROM user WHERE id = #{id}
	</delete>

</mapper>
