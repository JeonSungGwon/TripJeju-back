<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.jeju.member.model.mapper.MemberMapper">

	<insert id="joinMember" parameterType="member">
		insert into member (user_name, user_id, user_password, user_email, join_date)
		values (#{userName}, #{userId}, #{userPassword}, #{userEmail}, now())
	</insert>

	<select id="loginMember" parameterType="map">
		select user_name, user_id, user_password, user_email, join_date
		from member
		where user_id = #{userId} and user_password = #{userPassword}
	</select>
	
</mapper>